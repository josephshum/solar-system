{
  "name": "solar-system",
  "homepage": "https://josephshum.github.io/solar-system",
  "private": false,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "git add -f dist && git commit -m 'Deploy' && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@tweenjs/tween.js": "^23.1.2",
    "three": "^0.164.1"
  }
} 